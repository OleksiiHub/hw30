describe("Result of two numbers", () => {
    const desc = "<p>Result of two numbers</p>";

    describe(desc, () => {
        it("should return", () => {
            const resultArr = result(2, 2);
            expect(resultArr).toBe(4, 0, 8, 1);
        });

        it("should return ", () => {
            const resultArr = result(3, 3);
            expect(resultArr).toBe(console.log(1,1,1,1));
        });
    });
});

describe("Result of two numbers", () => {
    const desc = "<p>Result of two numbers</p>";

    describe(desc, () => {
        it("should return", () => {
            const resultArr = result(2, 2);
            expect(resultArr).toBe(4, 0, 8, 1);
        });

        it("should return ", () => {
            const resultArr = result(3, 3);
            expect(resultArr).toBe(console.log(1,1,1,1));
        });
    });
});